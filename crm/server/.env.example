DATABASE_URL=postgresql://user:password@localhost:5432/signal_ridge_crm
JWT_SECRET=change-me-in-production
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me
PORT=3001
GMAIL_CLIENT_ID=your-gmail-client-id
GMAIL_CLIENT_SECRET=your-gmail-client-secret
GMAIL_REDIRECT_URI=http://localhost:3001/auth/gmail/callback
ANTHROPIC_API_KEY=your-anthropic-api-key
# Optional: comma-separated CORS origins allowed to hit /api/bot/*. Leave
# blank to allow any origin (server-to-server bots don't enforce CORS anyway;
# this is only for browser-based clients).
BOT_CORS_ORIGINS=
