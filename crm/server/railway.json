{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app/crm/client && npm install && npm run build && cd /app/crm/server && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "node dist/src/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
